# 📦 Fetch_Data_From_Api

> A simple Python project to fetch data from an API endpoint and export it to a CSV file.

This repository demonstrates how to call an API, retrieve JSON data, and convert it into a CSV format using Python (in a Jupyter Notebook).

---

## 🚀 Project Overview

This project:
- Fetches data from a specified API endpoint.
- Parses JSON responses.
- Normalizes and saves the data into a structured **CSV file**.
- Uses Python inside a Jupyter Notebook (`practice.ipynb`) for implementation.

The main goal is to help beginners understand how to work with HTTP APIs and export results for further use.

---

## 📁 Repository Structure

